{
  "name": "react-component-hover-inspector",
  "version": "1.5.1",
  "private": true,
  "description": "A local-development Chrome extension that identifies React components on hover.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "dev": "node scripts/build.mjs --watch",
    "test": "npm run build && node test/background-injection.mjs && node test/component-resolution.mjs && node test/content-navigation.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
